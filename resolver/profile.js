// Empty resolver file for the profile query

const resolvers = {
    Query: {
      profile: (parent, args, context, info) => {
        // Add your logic here to fetch and return the profile data
      },
    },
  };
  
  module.exports = resolvers;
  