const resolvers = {
    Query: {
      canClaim: (_, { request }) => {
        
      },
    },
    CanClaim: {
      address: (parent) => {
      
      },
      canClaim: (parent) => {
       
      },
    },
  };
  
  module.exports = resolvers;
  